[pytest]
testpaths = tests
addopts = -ra
markers =
    integration: lightweight runtime-import tests with CPU model dependencies
    smoke: requires a real released OpticalDNA checkpoint and is excluded from normal CI
